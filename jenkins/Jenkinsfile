// Scripted CI. Shared library `zerotrust` (jenkins/shared-library).
// After merge to main: this job runs. Argo CD + Argo Rollouts deploy — Jenkins does not.
@Library('zerotrust') _

zerotrustPipeline(
  chartPath: 'charts/zerotrust-apps',
  clusters: ['eks-sim', 'aks-sim'],
  registry: '172.18.0.20:5000',
  imageName: 'zerotrust-backend',
  dockerfile: 'app/backend/Dockerfile',
  dockerContext: 'app/backend',
  sonarHostUrl: 'http://sonarqube.sonarqube.svc.cluster.local:9000'
)
